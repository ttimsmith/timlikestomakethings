= content_for :title, 'Comments'
= content_for :subtitle, "#{@post.title}"

- if @comments.present?
  %table
    %thead
      %tr
        %th{ colspan: '2', width: '650' } Author
        %th Comment
    %tbody
      = render @comments
- else
  %p There are no comments yet
