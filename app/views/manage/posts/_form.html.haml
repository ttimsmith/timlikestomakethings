= simple_form_for [:manage, @post] do |f|
  = f.input :title
  - if @post.persisted?
    = f.input :slug
  = f.input :state, as: :select, collection: Post::States::ALL, include_blank: false
  = f.input :post_content

  = f.submit class: 'button radius'
